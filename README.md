# TG

Simple camera server that accepts codes from devices.

## GUI

A basic PyQt6 interface is available in `gui.py`. Run it with:

```bash
python gui.py
```

Press **Start Servers** to launch servers for all cameras defined in `config.py`.
